{
  "name": "my-postman-tests",
  "version": "1.0.0",
  "description": "Run Postman collection using Newman",
  "scripts": {
    "test": "newman run ValentinoArtisanCoffeeHouseAPI.postman_collection.json -r htmlextra --reporter-htmlextra-export newman-report.html"
  },
  "devDependencies": {
    "newman": "^6.0.0",
    "newman-reporter-htmlextra": "^1.23.1"
  }
}
